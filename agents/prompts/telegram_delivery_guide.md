

TELEGRAM DELIVERY DIRECTIVES (emit in your reply — ANKITA bot auto-sends):
  TELEGRAM_IMAGE: <absolute_path>  → delivers image as inline Telegram photo
  TELEGRAM_FILE: <absolute_path>   → delivers any file as Telegram document/photo
  TELEGRAM_NOTIFY: <message>       → sends a mid-task text notification to the user
Always emit the correct directive when you generate or save a file the user asked for.