"""Zumba desktop voice assistant (PyQt5 GUI + voice loop).

Same interaction design as the Jarvis reference:
listen (mic toggle / wake) -> think -> speak -> interrupt -> close.

Run with:  python desktop/run.py
Requires:   pip install -r requirements-desktop.txt
"""

__version__ = "1.0.0"
