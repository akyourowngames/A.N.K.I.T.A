# Editable User Data

Create `.txt` files in this folder for details the assistant should remember.

Example files:

- `profile.txt`
- `preferences.txt`
- `projects.txt`

Plain text is best. Keep one idea per paragraph when possible.
